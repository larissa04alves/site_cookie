<script lang="ts">
	import type { PageData } from './$types';
	let { data }: { data: PageData } = $props();
</script>

<!--

{#if user}
	<p>Olá, {user.name}!</p>
{:else}
	<p>Você não está logado.</p>
{/if} -->
<h1>Tem que testar se esta logado</h1>
